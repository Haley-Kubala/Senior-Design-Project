import re
def check_string():
    id = raw_input("Enter ID")

    #opening the file using `with` context manager, bc it'll automatically close the file for you apparently
    with open("studentID.txt") as f:
        found = False
        for line in f:  #iterate over the file one line at a time(memory efficient)
            if re.search("\b{0}\b".format(w),line):    #if string found is in current line then print it
                print line
                found = True
        if not found:
            print('ID UNKNOWN')
